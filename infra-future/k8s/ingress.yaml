apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: wary-ingress
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "10m"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "60"
    nginx.ingress.kubernetes.io/use-regex: "true"
    nginx.ingress.kubernetes.io/rewrite-target: /$2
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: nginx
  rules:
  - host: wary.dev
    http:
      paths:
      - path: /api(/|$)(.*)
        pathType: ImplementationSpecific
        backend:
          service:
            name: wary-backend
            port:
              number: 8000
      - path: /(/?)(.*)
        pathType: ImplementationSpecific
        backend:
          service:
            name: wary-frontend
            port:
              number: 80
  tls:
  - hosts:
    - wary.dev
    secretName: wary-tls
    # TLS certificate is managed by cert-manager via the cluster-issuer annotation above
    # To use a pre-existing secret, remove the cert-manager annotation and ensure the
    # secret 'wary-tls' exists with tls.crt and tls.key entries
