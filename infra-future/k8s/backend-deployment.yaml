apiVersion: apps/v1
kind: Deployment
metadata:
  name: wary-backend
  labels:
    app: wary
    component: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: wary
      component: backend
  template:
    metadata:
      labels:
        app: wary
        component: backend
    spec:
      containers:
      - name: backend
        image: wary/backend:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8000
        envFrom:
        - configMapRef:
            name: wary-config
        - secretRef:
            name: wary-secrets
        startupProbe:
          httpGet:
            path: /live
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 5
          failureThreshold: 30
        livenessProbe:
          httpGet:
            path: /live
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /ready
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 15
        securityContext:
          readOnlyRootFilesystem: true
          runAsNonRoot: true
          runAsUser: 1000
          runAsGroup: 1000
          allowPrivilegeEscalation: false
          capabilities:
            drop:
              - ALL
            add:
              - NET_BIND_SERVICE
        volumeMounts:
        - name: tmp
          mountPath: /tmp
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
      volumes:
      - name: tmp
        emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: wary-backend
  labels:
    app: wary
    component: backend
spec:
  selector:
    app: wary
    component: backend
  ports:
  - port: 8000
    targetPort: 8000
  type: ClusterIP
